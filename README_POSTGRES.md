# Настройка Neon Postgres для Telegram бота

## ✅ Что сделано

1. ✅ Создана миграция `migrations/001_create_tables.sql` для PostgreSQL
2. ✅ Обновлен `config.py` для автоматического определения PostgreSQL
3. ✅ Обновлен `database.py` для поддержки PostgreSQL и SQLite
4. ✅ Создан скрипт `apply_migration.py` для применения миграции

## 🚀 Быстрый старт

### 1. Применить миграцию

После добавления Neon Postgres в Vercel, примените миграцию:

**Вариант 1: Через скрипт (локально)**
```bash
# Убедитесь, что у вас есть переменные окружения
# POSTGRES_URL или POSTGRES_PRISMA_URL из Vercel Dashboard
python apply_migration.py
```

**Вариант 2: Через Vercel Dashboard**
1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в Storage → ваша база данных
3. Откройте SQL Editor
4. Скопируйте содержимое `migrations/001_create_tables.sql`
5. Выполните SQL

### 2. Проверка

После применения миграции:
- ✅ Таблицы созданы в PostgreSQL
- ✅ Бот автоматически использует PostgreSQL на Vercel
- ✅ Локально продолжает использовать SQLite

## 📝 Важные замечания

### Имена таблиц

В PostgreSQL используются таблицы с префиксом `telegram_`:
- `telegram_users` (вместо `users`)
- `telegram_promocodes` (вместо `promocodes`)
- `telegram_calculations` (вместо `calculations`)
- и т.д.

Код автоматически выбирает правильные имена таблиц в зависимости от типа БД.

### Переменные окружения

Vercel автоматически добавляет:
- `POSTGRES_PRISMA_URL` - для Prisma (с pgbouncer)
- `POSTGRES_URL` - прямой URL
- `POSTGRES_URL_NON_POOLING` - для миграций

Код использует `POSTGRES_PRISMA_URL` или `POSTGRES_URL` автоматически.

## 🔧 Обновленные методы

Следующие методы теперь работают с PostgreSQL:
- ✅ `add_user()` - сохранение пользователей
- ✅ `get_user()` - получение пользователя
- ✅ `create_promocode()` - создание промокодов
- ✅ `get_promocode()` - получение промокода
- ✅ `is_admin()` - проверка прав администратора

Остальные методы будут обновлены по мере необходимости.

## 🐛 Отладка

Если что-то не работает:

1. Проверьте логи в Vercel Dashboard → Functions → Logs
2. Убедитесь, что миграция применена
3. Проверьте переменные окружения в Vercel Dashboard
4. Попробуйте применить миграцию еще раз

## 📚 Дополнительно

См. `VERCEL_POSTGRES_SETUP.md` для подробной инструкции.

