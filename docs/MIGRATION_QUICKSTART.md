# 🚀 Быстрое применение миграции

## Самый простой способ ⭐

1. **Запустите Next.js приложение:**
   ```bash
   npm run dev
   ```

2. **Откройте в браузере:**
   ```
   http://localhost:3000/migrate
   ```

3. **Скопируйте SQL** (кнопка "Копировать SQL")

4. **Откройте Supabase Dashboard:**
   - https://supabase.com/dashboard
   - Выберите проект
   - SQL Editor (в боковом меню)

5. **Вставьте SQL и нажмите Run** ✅

## Альтернатива: прямое применение

Если у вас есть `SUPABASE_DB_URL`:

```bash
# Добавьте в .env.local:
SUPABASE_DB_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres

# Затем запустите:
npm run migrate
```

## Что создаст миграция?

- ✅ Все таблицы для Telegram бота
- ✅ Индексы для оптимизации
- ✅ Связи между таблицами (foreign keys)

**Готово!** После применения миграции ваше Next.js приложение сможет работать с базой данных.
