# Быстрый старт на Vercel

## ✅ Вариант 1: Автоматическое определение (рекомендуется)

Код автоматически определит Vercel и использует `/tmp/bot_database.db`. **Не нужно** устанавливать `DATABASE_PATH`!

## Шаги настройки:

### 1. Деплой на Vercel

```bash
vercel
```

Или подключите GitHub репозиторий через веб-интерфейс Vercel.

### 2. Установите только 3 переменные окружения в Vercel:

В настройках проекта Vercel → Environment Variables добавьте:

| Переменная | Значение | Пример |
|------------|----------|--------|
| `BOT_TOKEN` | Токен вашего Telegram бота | `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz` |
| `DEEPSEEK_API_KEY` | API ключ DeepSeek | `sk-...` |
| `ADMIN_IDS` | ID администраторов через запятую | `123456789,987654321` |

**⚠️ Важно для ADMIN_IDS:**
- БЕЗ пробелов: `123456789,987654321` ✅
- С пробелами: `123456789, 987654321` ❌ (будет ошибка)

### 3. Получите URL вашего приложения

После деплоя вы получите URL вида:
```
https://your-app-name.vercel.app
```

### 4. Установите webhook в Telegram

```bash
python scripts/setup_webhook.py https://your-app-name.vercel.app/api/webhook
```

Или вручную через curl:
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://your-app-name.vercel.app/api/webhook"
```

### 5. Проверьте webhook

```bash
python scripts/setup_webhook.py info
```

Вы должны увидеть:
```
📋 Информация о webhook:
   URL: https://your-app-name.vercel.app/api/webhook
   Ожидающих обновлений: 0
```

### 6. Готово! 🎉

Отправьте боту сообщение `/start` в Telegram - он должен ответить!

## ⚠️ Важное предупреждение о базе данных

На Vercel используется `/tmp/bot_database.db` - это **временная директория** (ephemeral storage).

**Данные будут теряться:**
- При каждом перезапуске функции
- При деплое новой версии
- После периода простоя функции

**Для продакшена рекомендуется:**
- Использовать внешнюю базу данных (Supabase, PlanetScale, Railway)
- Или настроить резервное копирование данных

## Устранение проблем

### Бот не отвечает

1. Проверьте логи: Vercel Dashboard → Deployments → Functions → View Function Logs
2. Убедитесь, что webhook установлен: `python scripts/setup_webhook.py info`
3. Проверьте переменные окружения в Vercel
4. Убедитесь, что URL правильный: `https://your-app-name.vercel.app/api/webhook`

### Ошибки базы данных

Если видите ошибки типа "read-only file system" или "permission denied":
- Убедитесь, что `DATABASE_PATH` НЕ установлена в переменных окружения
- Код автоматически использует `/tmp/bot_database.db` на Vercel

## Возврат к polling (локальная разработка)

Если хотите запустить бота локально с polling:

```bash
# Удалите webhook
python scripts/setup_webhook.py delete

# Запустите бота локально
python -m src.bot
```

Бот будет использовать `data/bot_database.db` локально (постоянное хранилище).

## Дополнительная информация

Подробная документация: `VERCEL_SETUP.md`

