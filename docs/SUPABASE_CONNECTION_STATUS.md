# 📊 Статус подключения к Supabase

## ✅ Что работает

### 1. REST API подключение
- **SUPABASE_URL**: ✅ Настроен (`https://ouodquakgyyeiyihmoxg.supabase.co`)
- **SUPABASE_API_KEY**: ✅ Настроен (service_role ключ)
- **Статус**: ✅ **РАБОТАЕТ**

Бот может использовать Supabase через REST API для всех операций с базой данных.

### 2. Конфигурация
- Переменные окружения загружаются из `.env` файла
- Код автоматически определяет тип подключения (REST API или PostgreSQL)
- Поддержка обоих методов подключения реализована

## ⚠️ Что не работает

### PostgreSQL прямое подключение
- **SUPABASE_DB_URL**: ✅ Настроен
- **Формат**: Direct connection (`db.ouodquakgyyeiyihmoxg.supabase.co:5432`)
- **Проблема**: ❌ DNS не резолвится (`getaddrinfo failed`)

**Возможные причины:**
1. Неправильный project reference в URL
2. Проблемы с интернет-соединением
3. Хост недоступен из вашей сети

## 🔧 Решения

### Вариант 1: Использовать REST API (рекомендуется)
REST API подключение **уже работает**! Бот может использовать его для всех операций.

**Преимущества:**
- ✅ Уже настроено и работает
- ✅ Не требует прямого доступа к PostgreSQL
- ✅ Автоматическая обработка соединений
- ✅ Безопасность через API ключи

**Недостатки:**
- ⚠️ Для применения миграций нужен прямой доступ к PostgreSQL
- ⚠️ Немного медленнее, чем прямое подключение

### Вариант 2: Исправить PostgreSQL подключение

#### Проверьте формат SUPABASE_DB_URL:

**Для Direct connection:**
```
postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres
```

**Для Connection pooling:**
```
postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
```

**Где найти правильный URL:**
1. Откройте [Supabase Dashboard](https://app.supabase.com/)
2. Выберите ваш проект
3. Перейдите в **Settings → Database**
4. Скопируйте **Connection string** (используйте **Direct connection** для миграций)

#### Применить миграцию через Supabase Dashboard:

1. Откройте [Supabase Dashboard](https://app.supabase.com/)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Скопируйте содержимое файла `migrations/001_create_tables.sql`
5. Вставьте в SQL Editor и выполните

## 📝 Текущая конфигурация

```env
SUPABASE_URL=https://ouodquakgyyeiyihmoxg.supabase.co
SUPABASE_API_KEY=eyJ... (service_role)
SUPABASE_DB_URL=postgresql://postgres:***@db.ouodquakgyyeiyihmoxg.supabase.co:5432/postgres
```

## ✅ Рекомендации

1. **Используйте REST API** для работы бота - он уже настроен и работает
2. **Примените миграцию** через Supabase Dashboard → SQL Editor
3. **Проверьте SUPABASE_DB_URL** - возможно, нужно использовать Connection pooling вместо Direct connection

## 🧪 Проверка подключения

Запустите диагностику:
```bash
python scripts/diagnose_supabase.py
```

Проверьте REST API:
```bash
python test_supabase_connection.py
```

## 📚 Дополнительная информация

- [Документация Supabase](https://supabase.com/docs)
- [QUICK_START_SUPABASE.md](./QUICK_START_SUPABASE.md)
- [SUPABASE_SETUP.md](./SUPABASE_SETUP.md)
