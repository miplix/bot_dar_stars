# Настройка Vercel Postgres для Telegram бота

## Проблема с SQLite на Vercel

SQLite на Vercel не работает надежно, потому что:
- Данные в `/tmp` **временные** и теряются при каждом перезапуске функции
- Каждый запрос может попасть на **другой инстанс**, поэтому данные не синхронизируются

## Решение: Vercel Postgres

Vercel предлагает **Vercel Postgres** (Neon) - полноценную PostgreSQL базу данных, которая:
- ✅ Постоянно хранит данные
- ✅ Работает с несколькими инстансами serverless функций
- ✅ Поддерживает транзакции и связи между таблицами
- ✅ Легко настраивается через Vercel Dashboard

## Настройка

### 1. Создание базы данных на Vercel

1. Откройте проект на [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в раздел **Storage** → нажмите **Select Existing** (или используйте Marketplace)
3. Или перейдите в **Marketplace** (в боковом меню) → найдите **Postgres**
4. Нажмите **Add Integration** → выберите **Neon Postgres**
5. Выберите регион (ближайший к вашим пользователям)
6. Создайте базу данных

**Альтернативный способ:**
- Откройте [Vercel Marketplace](https://vercel.com/marketplace)
- Найдите "Neon Postgres" или "Vercel Postgres"
- Нажмите **Add Integration**
- Выберите проект и создайте базу данных

### 2. Установка переменных окружения

После создания БД Vercel автоматически добавит переменные окружения:
- `POSTGRES_PRISMA_URL` - для Prisma (если используете)
- `POSTGRES_URL` - прямой URL для подключения
- `POSTGRES_URL_NON_POOLING` - для миграций

Код автоматически использует эти переменные.

### 3. Применение миграций

Миграция уже создана в файле `migrations/001_create_tables.sql`.

Для применения миграции:

**Вариант 1: Через Vercel Dashboard**
1. Откройте вашу базу данных в Vercel Dashboard
2. Перейдите в раздел **SQL Editor**
3. Скопируйте содержимое `migrations/001_create_tables.sql`
4. Выполните SQL запрос

**Вариант 2: Через psql (локально)**
```bash
# Установите PostgreSQL client (если еще не установлен)
# Windows: choco install postgresql
# Mac: brew install postgresql
# Linux: sudo apt-get install postgresql-client

# Получите POSTGRES_URL из Vercel Dashboard
psql "YOUR_POSTGRES_URL" < migrations/001_create_tables.sql
```

**Вариант 3: Через Python скрипт**
```python
# create_tables.py
import asyncio
import asyncpg
import os
from dotenv import load_dotenv

load_dotenv()

async def main():
    database_url = os.getenv('POSTGRES_URL') or os.getenv('POSTGRES_PRISMA_URL')
    if not database_url:
        print("❌ DATABASE_URL не установлен!")
        return
    
    # Удаляем параметры pgbouncer из URL если есть
    conn_url = database_url.replace('?pgbouncer=true', '').split('?')[0]
    
    conn = await asyncpg.connect(conn_url)
    
    with open('migrations/001_create_tables.sql', 'r', encoding='utf-8') as f:
        sql = f.read()
    
    await conn.execute(sql)
    await conn.close()
    
    print("✅ Таблицы созданы!")

if __name__ == '__main__':
    asyncio.run(main())
```

Запустите:
```bash
python create_tables.py
```

### 4. Переменные окружения

Убедитесь, что в Vercel установлены переменные окружения:

- ✅ `BOT_TOKEN` - токен Telegram бота
- ✅ `DEEPSEEK_API_KEY` - ключ DeepSeek AI
- ✅ `ADMIN_IDS` - ID администраторов через запятую
- ✅ `POSTGRES_URL` или `POSTGRES_PRISMA_URL` - автоматически добавляется Vercel при создании БД

### 5. Проверка

После настройки:
1. Деплойте бота на Vercel
2. Отправьте боту `/start` в Telegram
3. Проверьте, что пользователь сохранился в БД
4. Проверьте логи в Vercel Dashboard → Functions → Logs

## Преимущества Vercel Postgres

- 🚀 **Быстрое подключение** - оптимизировано для serverless
- 💾 **Постоянное хранилище** - данные не теряются
- 🔄 **Автоматические бэкапы** - встроенные функции резервного копирования
- 📊 **Мониторинг** - метрики использования в Vercel Dashboard
- 🔒 **Безопасность** - данные шифруются и изолированы

## Локальная разработка

Для локальной разработки код автоматически использует SQLite (`data/bot_database.db`).

Если хотите использовать PostgreSQL локально:
1. Установите PostgreSQL локально или используйте Docker
2. Создайте базу данных
3. Установите переменную окружения `DATABASE_URL=postgresql://user:pass@localhost:5432/bot_db`
4. Примените миграции

## Миграция с SQLite на PostgreSQL

Если у вас уже есть данные в SQLite:
1. Экспортируйте данные: `python scripts/export_db_data.py`
2. Примените миграции для PostgreSQL
3. Импортируйте данные (можно создать скрипт для импорта)

## Полезные ссылки

- [Vercel Postgres Documentation](https://vercel.com/docs/storage/vercel-postgres)
- [Neon Documentation](https://neon.tech/docs)
- [asyncpg Documentation](https://magicstack.github.io/asyncpg/)

