# 🚀 Быстрый старт с Supabase

## У вас уже есть Supabase проект! ✅

Ваш проект: `ouodquakgyyeiyihmoxg`

Все таблицы уже созданы в базе данных.

## 🎯 Два способа подключения:

### ✅ Способ 1: Через API ключ (РЕКОМЕНДУЕТСЯ - проще!)

Не нужен пароль базы данных, проще в настройке!

### Способ 2: Через строку подключения PostgreSQL

Требует пароль базы данных, но работает быстрее для сложных запросов.

---

## 📝 Способ 1: Через API ключ (ПРОЩЕ!)

### 1. Получить API ключ

1. Откройте [Supabase Dashboard](https://app.supabase.com/)
2. Выберите проект `ouodquakgyyeiyihmoxg`
3. Перейдите **Settings** → **API**
4. Найдите **Project API keys**
5. Скопируйте **`anon` key** (публичный ключ) или **`service_role` key** (для полного доступа)

**Рекомендация:** Для бота используйте **`service_role` key**, так как он обходит RLS политики.

### 2. Установить переменные окружения

#### Локально (`.env` файл):

```env
SUPABASE_URL=https://ouodquakgyyeiyihmoxg.supabase.co
SUPABASE_API_KEY=ваш_service_role_ключ_здесь
```

**Готово!** Не нужно получать пароль базы данных! 🎉

#### На Vercel:

1. Откройте проект в [Vercel Dashboard](https://vercel.com/dashboard)
2. **Settings** → **Environment Variables**
3. Добавьте:
   - `SUPABASE_URL` = `https://ouodquakgyyeiyihmoxg.supabase.co`
   - `SUPABASE_API_KEY` = ваш service_role ключ из шага 1

### 3. Готово! 🎉

Запустите бота - он автоматически подключится к Supabase через REST API!

При запуске вы увидите:
```
🔥 Используется Supabase через REST API (API ключ)
   URL: https://ouodquakgyyeiyihmoxg.supabase.co
```

---

## 📝 Способ 2: Через строку подключения PostgreSQL

### 1. Получить пароль базы данных

1. Откройте [Supabase Dashboard](https://app.supabase.com/)
2. Выберите проект `ouodquakgyyeiyihmoxg`
3. Перейдите **Settings** → **Database**
4. Найдите **Database password**
5. Если пароля нет, нажмите **Reset database password** и скопируйте новый пароль

### 2. Получить строку подключения

### 1. Получить пароль базы данных

1. Откройте [Supabase Dashboard](https://app.supabase.com/)
2. Выберите проект `ouodquakgyyeiyihmoxg`
3. Перейдите **Settings** → **Database**
4. Найдите **Database password**
5. Если пароля нет, нажмите **Reset database password** и скопируйте новый пароль

### 2. Получить строку подключения

В том же разделе **Settings** → **Database**:

1. Прокрутите до **Connection string**
2. Выберите **Connection pooling** → **Session mode**
3. Скопируйте **URI** (она начинается с `postgresql://postgres.ouodquakgyyeiyihmoxg...`)

Или для прямого подключения:

1. Выберите **Direct connection**
2. Скопируйте **URI** (она начинается с `postgresql://postgres@db...`)

**Рекомендация:** Используйте **Connection pooling** для продакшена, **Direct connection** для локальной разработки.

### 3. Установить переменные окружения

#### Локально (`.env` файл):

```env
SUPABASE_URL=https://ouodquakgyyeiyihmoxg.supabase.co
SUPABASE_DB_URL=postgresql://postgres.ouodquakgyyeiyihmoxg:[ВАШ_ПАРОЛЬ]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true
```

**Замените `[ВАШ_ПАРОЛЬ]`** на реальный пароль из шага 1.

#### На Vercel:

1. Откройте проект в [Vercel Dashboard](https://vercel.com/dashboard)
2. **Settings** → **Environment Variables**
3. Добавьте:
   - `SUPABASE_URL` = `https://ouodquakgyyeiyihmoxg.supabase.co`
   - `SUPABASE_DB_URL` = ваша строка подключения из шага 2

### 4. Готово! 🎉

Запустите бота - он автоматически подключится к Supabase!

При запуске вы увидите:
```
🔥 Используется Supabase (прямое подключение PostgreSQL)
   URL: https://ouodquakgyyeiyihmoxg.supabase.co
✅ Таблицы Supabase найдены
```

---

## ⚡ Какой способ выбрать?

- **Через API ключ** (Способ 1): Проще в настройке, не нужен пароль БД
- **Через строку подключения** (Способ 2): Быстрее для сложных SQL запросов, нужен пароль БД

**Рекомендация:** Начните со Способа 1 (API ключ) - это проще!

## Проверка работы

1. Запустите бота
2. Отправьте `/start` в боте
3. Откройте Supabase Dashboard → **Table Editor** → `telegram_users`
4. Вы должны увидеть нового пользователя!

## Проблемы?

Смотрите полную документацию: [SUPABASE_SETUP.md](./SUPABASE_SETUP.md)
