# 🔄 Обновление системы: Админы и промокоды

## ✅ Что нового

Добавлены две мощные системы:

### 1. 👑 Административные права
- Безлимитный доступ для админов
- Управление через команду `/admin`
- Генерация промокодов
- Статистика бота

### 2. 🎁 Система промокодов
- Промокоды на бесплатную подписку
- Промокоды со скидками
- Лимиты использований
- Автоматическая генерация кодов

---

## 🚀 Что нужно сделать

### Шаг 1: База данных обновится автоматически

При запуске бота база данных автоматически обновится:
- ✅ Добавится поле `is_admin` в таблицу `users`
- ✅ Создастся таблица `promocodes`
- ✅ Создастся таблица `promocode_usage`

**Ничего делать не нужно!** Просто запустите бота.

### Шаг 2: Настройте администраторов

Добавьте ID админов в файл `.env`:

```env
ADMIN_IDS=123456789,987654321
```

Подробная инструкция: **НАСТРОЙКА_АДМИНОВ.md**

### Шаг 3: Перезапустите бота

```bash
./run.sh
```

### Шаг 4: Проверьте работу

```
/admin
```

Если появилась админ-панель - всё работает! ✅

---

## 📋 Обратная совместимость

✅ **Все старые функции работают** - ничего не сломано  
✅ **Старые пользователи сохранены** - все данные на месте  
✅ **Подписки не сбросились** - все подписки остались активными  

---

## 🆕 Новые файлы

Добавлены файлы документации:
- `АДМИН_И_ПРОМОКОДЫ.md` - полная документация
- `НАСТРОЙКА_АДМИНОВ.md` - быстрая настройка
- `ПРОМОКОДЫ_ДЛЯ_ПОЛЬЗОВАТЕЛЕЙ.md` - инструкция для пользователей
- `ОБНОВЛЕНИЕ_СИСТЕМЫ.md` - этот файл

---

## 🔧 Технические изменения

### Изменены файлы:
- `database.py` - добавлены методы для админов и промокодов
- `bot.py` - добавлены обработчики и команды
- `keyboards.py` - новые клавиатуры для админки
- `config.py` - добавлена настройка ADMIN_IDS

### Новые функции в коде:

```python
# Проверка админа
is_admin = await db.is_admin(user_id)

# Проверка подписки с учетом админов
subscription = await check_subscription_with_admin(user_id)

# Создание промокода
await db.create_promocode(...)

# Активация промокода
await db.use_promocode(user_id, promo_id)
```

---

## 📊 Новая структура БД

### Таблица `users` (изменена)
```sql
+ is_admin INTEGER DEFAULT 0
```

### Таблица `promocodes` (новая)
```sql
- id
- code (уникальный)
- type (subscription/discount)
- discount_percent
- subscription_days
- max_uses
- current_uses
- created_date
- created_by
- is_active
```

### Таблица `promocode_usage` (новая)
```sql
- id
- promocode_id
- user_id
- usage_date
```

---

## 💡 Быстрый старт

Если вы первый раз запускаете новую версию:

1. **Остановите бота** (если запущен)
   ```bash
   Ctrl+C
   ```

2. **Добавьте свой ID в .env**
   ```env
   ADMIN_IDS=ваш_telegram_id
   ```

3. **Запустите бота**
   ```bash
   ./run.sh
   ```

4. **Откройте админ-панель**
   ```
   /admin
   ```

5. **Создайте первый промокод**
   - Выберите тип
   - Укажите значение
   - Установите лимит
   - Готово! 🎉

---

## 📞 Поддержка

Если что-то пошло не так:

1. Проверьте логи бота
2. Убедитесь что база данных обновилась
3. Проверьте права доступа к файлам
4. Перезапустите бота

Проблема сохраняется? Смотрите `АДМИН_И_ПРОМОКОДЫ.md` для детальной информации.

---

## 🎉 Готово!

Теперь у вас полноценная система администрирования с промокодами!

**Что можно делать:**
- ✅ Выдавать админские права
- ✅ Создавать промокоды на подписки
- ✅ Делать промокоды со скидками
- ✅ Проводить акции и розыгрыши
- ✅ Отслеживать статистику

Успешной работы! 🚀

