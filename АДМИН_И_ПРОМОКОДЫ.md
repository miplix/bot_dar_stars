# 👑 Административные права и промокоды

## ✅ Что добавлено

Полная система администрирования с промокодами для скидок и бесплатных подписок.

---

## 👑 Административные права

### Что дает статус админа?

✅ **Безлимитный доступ** - админы имеют неограниченный доступ ко всем функциям бота без подписки  
✅ **Админ-панель** - доступ к управлению промокодами через команду `/admin`  
✅ **Генерация промокодов** - создание промокодов со скидками или бесплатными подписками  
✅ **Статистика** - просмотр статистики использования бота

### Как назначить администратора?

Добавьте ID пользователей в файл `.env`:

```env
ADMIN_IDS=123456789,987654321
```

**Важно:** ID разделяются запятыми без пробелов.

### Где взять ID пользователя?

1. Попросите пользователя написать боту @userinfobot
2. Или используйте @getidsbot
3. ID будет в формате числа, например: `123456789`

---

## 🎁 Система промокодов

### Типы промокодов

#### 1. 🎁 Подписка
- Выдает бесплатную подписку на заданное количество дней
- Можно установить количество дней (например, 30, 90, 365)
- Активируется сразу после ввода

#### 2. 💰 Скидка
- Дает скидку в процентах на следующую покупку
- Скидка от 1% до 100%
- Применяется один раз при оплате

### Ограничения использования

- **Безлимит** - промокод может использовать неограниченное количество людей
- **Лимитированный** - можно задать максимальное количество активаций (например, 100 человек)
- **Один раз на пользователя** - каждый пользователь может активировать промокод только один раз

### Формат промокода

Промокоды генерируются автоматически:
- Длина: **12 символов**
- Формат: `ABCD-EFGH-1234`
- Символы: буквы A-Z и цифры 2-9 (исключены похожие символы O, I, 0, 1, l)

---

## 🎯 Как использовать

### Для администраторов

#### 1. Открыть админ-панель
```
/admin
```

#### 2. Создать промокод
1. Нажмите "➕ Создать промокод"
2. Выберите тип:
   - 🎁 **Подписка** - бесплатная подписка
   - 💰 **Скидка** - скидка на оплату
3. Введите значение:
   - Для подписки: количество дней (например: `30`)
   - Для скидки: процент (например: `20`)
4. Укажите лимит использований:
   - `0` - безлимит
   - `100` - можно активировать 100 раз

#### 3. Просмотреть список промокодов
```
/admin → 📋 Список промокодов
```

Покажет все созданные промокоды с:
- ✅/❌ Статус активности
- Тип и значение
- Количество использований

#### 4. Просмотреть статистику
```
/admin → 📊 Статистика
```

Покажет:
- Количество пользователей по типам подписок
- Количество активных подписок
- Общую статистику

### Для пользователей

#### Активация промокода

1. Зайдите в раздел **💎 Подписка**
2. Нажмите **🎁 Ввести промокод**
3. Отправьте код (например: `ABCD-EFGH-1234`)

**Результат зависит от типа:**

##### 🎁 Промокод на подписку
```
✅ Промокод активирован!

🎉 Вам выдана подписка на 30 дней!
💫 Действительна до: 07.02.2026 12:00

Теперь у вас безлимитный доступ ко всем функциям бота!
```

##### 💰 Промокод на скидку
```
✅ Промокод активирован!

💰 Вам доступна скидка 20% на следующую покупку!

Перейдите в меню оформления подписки для применения скидки.
```

---

## 📊 База данных

### Новые таблицы

#### `promocodes` - Промокоды
```sql
- code - код промокода (уникальный)
- type - тип (subscription/discount)
- discount_percent - процент скидки (для discount)
- subscription_days - дни подписки (для subscription)
- max_uses - максимум использований (NULL = безлимит)
- current_uses - текущее количество использований
- created_date - дата создания
- created_by - ID админа создавшего
- is_active - активен ли промокод
```

#### `promocode_usage` - История использований
```sql
- promocode_id - ID промокода
- user_id - ID пользователя
- usage_date - дата использования
```

### Изменения в таблице `users`
```sql
+ is_admin - флаг администратора (0/1)
```

---

## 🔧 API методы (для разработки)

### Административные

```python
# Проверка прав админа
is_admin = await db.is_admin(user_id)

# Выдать права админа
await db.set_admin(user_id, True)

# Снять права админа
await db.set_admin(user_id, False)

# Получить список админов
admins = await db.get_all_admins()
```

### Промокоды

```python
# Создать промокод со скидкой
await db.create_promocode(
    code="ABCD1234EFGH",
    promo_type="discount",
    created_by=admin_id,
    discount_percent=20,
    max_uses=100
)

# Создать промокод с подпиской
await db.create_promocode(
    code="ABCD1234EFGH",
    promo_type="subscription",
    created_by=admin_id,
    subscription_days=30,
    max_uses=None  # Безлимит
)

# Получить промокод
promo = await db.get_promocode("ABCD1234EFGH")

# Проверить, использовал ли пользователь промокод
used = await db.check_user_used_promocode(user_id, promo_id)

# Зарегистрировать использование
await db.use_promocode(user_id, promo_id)

# Деактивировать промокод
await db.deactivate_promocode("ABCD1234EFGH")

# Получить все промокоды
promos = await db.get_all_promocodes()

# Статистика промокода
stats = await db.get_promocode_stats("ABCD1234EFGH")
```

### Проверка подписки с учетом прав админа

```python
# Вместо db.check_subscription используйте:
subscription = await check_subscription_with_admin(user_id)

# Админы автоматически получают:
# {"active": True, "type": "admin", "end_date": None}
```

---

## 💡 Примеры использования

### Пример 1: Выдать промокод на 7 дней новым пользователям

1. Создайте промокод:
   - Тип: **Подписка**
   - Дни: `7`
   - Лимит: `0` (безлимит)
2. Результат: `WELCOME7DAYS`
3. Разместите код в рекламе/соцсетях

### Пример 2: Скидка 50% для первых 50 покупателей

1. Создайте промокод:
   - Тип: **Скидка**
   - Процент: `50`
   - Лимит: `50`
2. Результат: `FIRST50SALE`
3. После 50 активаций промокод перестанет работать

### Пример 3: VIP подписка на год

1. Создайте промокод:
   - Тип: **Подписка**
   - Дни: `365`
   - Лимит: `10` (только для VIP)
2. Результат: `VIPYEAR2026`
3. Выдайте код личным сообщением

---

## ⚠️ Важные моменты

1. **Промокоды регистронезависимые** - `ABC123` и `abc123` это один код
2. **Один промокод = одна активация на пользователя** - нельзя использовать дважды
3. **Админы не могут использовать промокоды** - у них и так безлимит
4. **Скидка применяется к следующей покупке** - сохраняется в сессии пользователя
5. **Подписка активируется мгновенно** - сразу после ввода кода

---

## 🔒 Безопасность

- ✅ Промокоды генерируются криптографически безопасным генератором (`secrets`)
- ✅ Проверка прав админа при каждом действии
- ✅ Защита от повторного использования промокодов
- ✅ Лимиты использований хранятся в БД
- ✅ История всех активаций сохраняется

---

## 🚀 Быстрый старт для админа

1. Добавьте свой ID в `.env`:
   ```env
   ADMIN_IDS=ваш_id
   ```

2. Перезапустите бота:
   ```bash
   ./run.sh
   ```

3. Откройте админ-панель:
   ```
   /admin
   ```

4. Создайте первый промокод и протестируйте!

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. ID админа правильно указан в `.env`
2. Бот перезапущен после изменения `.env`
3. База данных обновилась (новые таблицы созданы)

Если проблема сохраняется - обратитесь к разработчику.

