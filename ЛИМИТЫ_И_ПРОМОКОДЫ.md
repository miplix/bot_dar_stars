# ✅ Система лимитов на ИИ и типы промокодов

## Что реализовано

Полностью внедрена система ограничений для пользователей с trial периодом и расширены возможности промокодов.

## 📊 Лимиты для Trial периода

### Основные характеристики

- **Trial период**: 7 дней с момента регистрации
- **Лимит запросов к ИИ**: 5 обработок
- **Счетчик**: Ведется в поле `ai_requests_count` таблицы `users`
- **Применяется к**:
  - Расчет даров (базовый)
  - Комплексный расчет профиля
  - Анализ сантр
  - Анализ слов через алфавит

### Где НЕ применяется

- Все платные подписки (test, month, year) дают **безлимитный доступ**
- Промокоды типа **Pro** дают безлимитный доступ
- Администраторы имеют безлимитный доступ

## 🎟 Типы промокодов

### Trial промокоды (🎁)

- Дают подписку с лимитом 5 запросов к ИИ
- Используются для тестирования или временного доступа
- При активации счетчик НЕ сбрасывается

### Pro промокоды (⭐️)

- Дают подписку с безлимитным доступом к ИИ
- Счетчик запросов к ИИ **сбрасывается** при активации
- Эквивалент платной подписки

## 🔄 Механизм работы

### При обращении к ИИ

1. Проверяется тип подписки пользователя
2. Если `trial` - проверяется счетчик `ai_requests_count`
3. Если лимит исчерпан - показывается сообщение с предложением оформить подписку
4. Если лимит не исчерпан - счетчик увеличивается на 1, выполняется запрос

### При оплате подписки

- Счетчик `ai_requests_count` сбрасывается в 0
- Пользователь получает безлимитный доступ на период подписки

### При использовании промокода

**Trial промокод:**
- Выдается подписка на N дней
- Тип: `trial`
- Счетчик НЕ сбрасывается
- Лимит: 5 запросов к ИИ

**Pro промокод:**
- Выдается подписка на N дней
- Тип: `pro` (сохраняется как `premium_promo`)
- Счетчик сбрасывается в 0
- Лимит: безлимит

## 💾 Изменения в базе данных

### Таблица `users`

Добавлено поле:
```sql
ai_requests_count INTEGER DEFAULT 0
```

### Таблица `promocodes`

Добавлено поле:
```sql
subscription_tier TEXT DEFAULT 'trial'
```

Возможные значения:
- `trial` - пробная подписка с лимитом
- `pro` - полная подписка без лимита

## 🔧 Новые функции в Database

```python
async def check_ai_limit(user_id: int) -> dict
    """
    Проверка лимита на запросы к ИИ
    Returns: {
        'allowed': bool,
        'current': int,
        'limit': int,
        'subscription_type': str
    }
    """

async def increment_ai_requests(user_id: int)
    """Увеличить счетчик запросов к ИИ"""

async def reset_ai_requests(user_id: int)
    """Сбросить счетчик запросов к ИИ"""

async def get_ai_requests_stats(user_id: int) -> dict
    """Получить статистику запросов к ИИ"""
```

## 👑 Создание промокодов админом

### Процесс создания

1. `/admin` → "➕ Создать промокод"
2. Выбор типа: "🎁 Подписка" или "💰 Скидка"
3. Ввод значения (дни или процент)
4. **Новое**: Выбор tier для подписки:
   - 🎁 Trial (лимит 5 запросов к ИИ)
   - ⭐️ Pro (безлимит)
5. Ввод лимита использований (0 = безлимит)
6. Промокод создан!

### Пример созданного промокода

```
✅ Промокод создан!

🎟 Код: ABCD1234EFGH
⭐️ Подписка Pro на 30 дней
🔢 10 использований

Пользователи могут ввести этот код в разделе подписок.
```

## 📝 Обновленные тексты

### Информация о подписках

Обновлен текст в `/subscription` и `subscription_info`:
- Добавлено упоминание лимита 5 запросов для trial
- Добавлено "безлимит" для платных подписок
- Показывается текущее использование лимита

### При исчерпании лимита

Пользователь видит:
```
⚠️ Лимит запросов к ИИ исчерпан

Вы использовали все 5 бесплатных анализов с ИИ 
в пробном периоде.

⭐️ Оформите подписку для безлимитного доступа:
📅 Месяц - 249 ⭐️
📆 Год - 2499 ⭐️

🎁 С подпиской вы получите:
• Безлимитные анализы с ИИ
• Полные трактовки даров
• Персональные рекомендации
• Все функции бота
```

## ⚙️ Конфигурация

### config.py

Добавлена константа:
```python
TRIAL_AI_LIMIT = 5  # Лимит запросов к ИИ для trial периода
```

Можно изменить для настройки лимита.

## 🧪 Тестирование

### Проверка лимита

1. Создайте нового пользователя
2. Выполните 5 анализов с ИИ (любые)
3. При 6-м запросе должно появиться сообщение о лимите

### Проверка промокодов

**Trial промокод:**
1. Создайте промокод Trial на 30 дней
2. Активируйте его
3. Проверьте, что лимит 5 запросов работает

**Pro промокод:**
1. Создайте промокод Pro на 30 дней
2. Активируйте его (счетчик должен сброситься)
3. Проверьте безлимитный доступ к ИИ

### Проверка платной подписки

1. Оплатите любую подписку
2. Счетчик должен сброситься
3. Доступ к ИИ безлимитный

## 📊 Мониторинг

Администратор может посмотреть:
- Статистику использования ИИ в админ-панели
- Список промокодов с типами (trial/pro)
- Количество активаций

## ⚠️ Важные моменты

1. **Trial период** - единственный тип подписки с лимитом
2. **Все оплаченные подписки** дают безлимит
3. **Pro промокоды** сбрасывают счетчик
4. **Trial промокоды** НЕ сбрасывают счетчик
5. **Администраторы** не имеют лимитов

## 🔄 Миграция данных

При первом запуске бота с новым кодом:
- Автоматически добавится поле `ai_requests_count` в таблицу users
- Автоматически добавится поле `subscription_tier` в таблицу promocodes
- Все существующие пользователи получат значение 0
- Все существующие промокоды получат tier 'trial'

## 📈 Статус реализации

✅ База данных обновлена
✅ Функции для работы с лимитами созданы
✅ Проверка лимита добавлена во все места вызова ИИ
✅ Тексты обновлены
✅ Админ-панель расширена выбором tier
✅ Обработка промокодов учитывает tier
✅ Бот перезапущен

Все задачи выполнены! Система полностью готова к использованию.

