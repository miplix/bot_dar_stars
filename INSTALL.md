# 📦 Инструкция по установке и запуску

## Шаг 1: Установка Python

Убедитесь, что у вас установлен Python 3.8 или выше:

```bash
python3 --version
```

## Шаг 2: Создание виртуального окружения

```bash
cd "/Users/macintosh/Documents/CURSOR_PROGRAMS/Бот с дарами за звезды"
python3 -m venv venv
```

## Шаг 3: Активация виртуального окружения

### macOS/Linux:
```bash
source venv/bin/activate
```

### Windows:
```bash
venv\Scripts\activate
```

## Шаг 4: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 5: Настройка конфигурации

Файл `.env` уже создан с вашим токеном бота. Для полной функциональности добавьте API ключ DeepSeek:

1. Зарегистрируйтесь на https://platform.deepseek.com/
2. Получите API ключ
3. Откройте файл `.env` и замените `your_deepseek_api_key_here` на ваш ключ

```env
DEEPSEEK_API_KEY=sk-ваш_ключ_здесь
```

## Шаг 6: Запуск бота

### Способ 1: Через скрипт (рекомендуется)
```bash
./run.sh
```

### Способ 2: Напрямую
```bash
python bot.py
```

## 🧪 Тестирование

После запуска бота:

1. Откройте Telegram
2. Найдите вашего бота (по токену)
3. Отправьте команду `/start`
4. Попробуйте рассчитать дары, введя дату рождения

## 🔧 Решение проблем

### Ошибка: "BOT_TOKEN не найден"
- Убедитесь, что файл `.env` существует в корневой директории проекта
- Проверьте, что токен правильно указан в `.env`

### Ошибка: "No module named 'aiogram'"
- Убедитесь, что виртуальное окружение активировано
- Переустановите зависимости: `pip install -r requirements.txt`

### База данных не создается
- Проверьте права доступа к директории `data/`
- Убедитесь, что директория `data/` существует

## 📊 Структура проекта после установки

```
.
├── venv/                  # Виртуальное окружение (создается автоматически)
├── data/                  # База данных
│   └── bot_database.db   # SQLite БД (создается при первом запуске)
├── bot.py                # Основной файл бота
├── config.py             # Конфигурация
├── database.py           # Работа с БД
├── calculations.py       # Расчет даров
├── ai_handler.py         # Интеграция с ИИ
├── keyboards.py          # Клавиатуры
├── gifts_knowledge.py    # База знаний о дарах
├── .env                  # Конфигурация (секретные данные)
├── .gitignore           # Игнорируемые файлы
├── requirements.txt      # Зависимости
├── README.md            # Документация
├── INSTALL.md           # Эта инструкция
└── run.sh               # Скрипт запуска
```

## 🚀 Быстрый старт (одной командой)

```bash
cd "/Users/macintosh/Documents/CURSOR_PROGRAMS/Бот с дарами за звезды" && ./run.sh
```

## 📝 Следующие шаги

1. ✅ Бот запущен и работает
2. 📚 Заполните базу знаний о дарах в файле `gifts_knowledge.py`
3. 🤖 Настройте DeepSeek API для полноценной работы ИИ
4. 💎 Реализуйте систему оплаты подписок
5. 🔮 Добавьте функцию гадания

## 💡 Полезные команды

### Остановка бота
Нажмите `Ctrl+C` в терминале

### Просмотр логов
Логи выводятся в консоль. Для сохранения в файл:
```bash
python bot.py > logs/bot.log 2>&1
```

### Резервное копирование БД
```bash
cp data/bot_database.db data/backup_$(date +%Y%m%d_%H%M%S).db
```

### Очистка БД (осторожно!)
```bash
rm data/bot_database.db
# БД будет пересоздана при следующем запуске
```


