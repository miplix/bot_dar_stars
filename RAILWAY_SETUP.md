# 🚂 Инструкция по настройке бота на Railway

## ⚠️ ВАЖНО: Перед деплоем на Railway

**ОБЯЗАТЕЛЬНО** остановите локальный экземпляр бота! Telegram не позволяет одному боту работать в двух местах одновременно.

## 📋 Шаги настройки

### 1. Создайте проект на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Создайте новый проект
4. Подключите ваш GitHub репозиторий

### 2. Настройте переменные окружения (Variables)

В настройках проекта Railway добавьте следующие переменные:

#### Обязательные переменные:

```bash
BOT_TOKEN=ваш_токен_бота_от_BotFather
```

#### Рекомендуемые переменные:

```bash
DEEPSEEK_API_KEY=ваш_ключ_deepseek_api
ADMIN_IDS=123456789,987654321
DATABASE_PATH=data/bot_database.db
DEEPSEEK_API_URL=https://api.deepseek.com/v1
```

**ВАЖНО для ADMIN_IDS:**
- Указывайте ID админов через запятую БЕЗ пробелов
- Пример: `123456789,987654321`
- НЕ правильно: `123456789, 987654321` (есть пробел)

### 3. Как узнать свой Telegram ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Он отправит вам ваш ID
3. Используйте этот ID в переменной ADMIN_IDS

### 4. Получение DeepSeek API ключа

1. Зарегистрируйтесь на [platform.deepseek.com](https://platform.deepseek.com)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте его в переменную DEEPSEEK_API_KEY

### 5. Деплой бота

После настройки переменных:
1. Railway автоматически задеплоит бота
2. Проверьте логи на наличие ошибок
3. Найдите в логах строки:
   ```
   ✅ Загружены админы: [ваши_id]
   ✅ DEEPSEEK_API_KEY загружен
   ```

## 🔍 Проверка работы

1. Откройте бота в Telegram
2. Отправьте команду `/start`
3. Попробуйте команду `/admin` (если вы админ)
4. Попробуйте рассчитать дары через кнопку "🎁 Рассчитать дары"

## ❌ Типичные ошибки

### Ошибка: "Conflict: terminated by other getUpdates request"

**Проблема:** Бот запущен одновременно локально и на Railway

**Решение:** 
- Остановите локальный экземпляр бота
- Перезапустите деплой на Railway

### Ошибка: "API ключ не настроен"

**Проблема:** Не установлена переменная DEEPSEEK_API_KEY

**Решение:**
- Добавьте DEEPSEEK_API_KEY в Variables на Railway
- Перезапустите деплой

### Ошибка: "Админы не работают"

**Проблема:** Неправильно указаны ADMIN_IDS

**Решение:**
- Проверьте формат: `123456789,987654321` (без пробелов!)
- Убедитесь, что используете правильные Telegram ID
- Проверьте логи: должна быть строка `✅ Загружены админы: [...]`

## 📊 Мониторинг логов

В Railway откройте вкладку "Logs" и найдите:

```
========================================
ЗАПУСК БОТА
========================================
BOT_TOKEN: ✅ Установлен
DEEPSEEK_API_KEY: ✅ Установлен
ADMIN_IDS: [123456789, 987654321]
...
✅ Админ 123456789 добавлен
✅ Админ 987654321 добавлен
========================================
Запуск бота...
========================================
```

Если видите ❌ или ⚠️ - проверьте переменные окружения.

## 🆘 Получение помощи

Если что-то не работает:
1. Проверьте логи на Railway
2. Убедитесь, что все переменные установлены правильно
3. Попробуйте перезапустить деплой

## 📝 Файлы для Railway

Проект уже содержит необходимые файлы:
- `Procfile` - команда запуска бота
- `runtime.txt` - версия Python
- `requirements.txt` - зависимости

Railway автоматически их распознает и использует.

